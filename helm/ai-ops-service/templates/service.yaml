apiVersion: v1
kind: Service
metadata:
  name: ai-ops-service
spec:
  selector:
    app: ai-ops-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

